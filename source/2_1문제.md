# PART 1. 객관식 70문제

## A. 원-핫 인코딩과 워드 임베딩

### 1.

원-핫 인코딩에서 단어를 표현하는 벡터의 특징으로 가장 적절한 것은?

① 모든 원소가 1이다.
② 한 원소만 1이고 나머지는 모두 0이다.
③ 모든 단어가 같은 벡터를 갖는다.
④ 단어의 의미에 따라 벡터 차원이 달라진다.

### 2.

원-핫 벡터의 차원 수는 일반적으로 무엇에 의해 결정되는가?

① 문장의 길이
② 은닉층의 개수
③ 단어 사전의 크기
④ 배치 크기

### 3.

서로 다른 두 단어의 원-핫 벡터 내적이 일반적으로 0이 되는 이유는?

① 두 벡터의 차원이 다르기 때문에
② 서로 다른 위치에만 1이 존재하여 두 벡터가 직교하기 때문에
③ 모든 원소가 음수이기 때문에
④ 정규화를 수행하기 때문에

### 4.

원-핫 인코딩의 문제점으로 가장 적절하지 않은 것은?

① 어휘가 커질수록 벡터 차원이 커진다.
② 고차원 희소 벡터가 되어 메모리 효율이 떨어질 수 있다.
③ 의미적으로 비슷한 단어의 유사성을 표현하기 어렵다.
④ 단어 간 의미적 유사성이 자동으로 잘 반영된다.

### 5.

원-핫 인코딩에서 '은행'과 '금융'처럼 의미적으로 가까운 단어가 서로 무관한 벡터로 취급되는 문제는 무엇과 가장 관련 있는가?

① 의미적 정보 부족
② 과적합
③ 데이터 누수
④ 배치 정규화

### 6.

워드 임베딩의 핵심 설명으로 가장 적절한 것은?

① 단어를 고차원 희소 벡터로만 표현한다.
② 단어를 의미적 관계를 포착할 수 있는 밀집되고 연속적인 벡터로 표현한다.
③ 모든 단어를 동일한 정수로 변환한다.
④ 단어의 순서를 무조건 제거한다.

### 7.

분포 가설(distributional hypothesis)의 핵심 아이디어는?

① 단어의 의미는 철자의 길이만으로 결정된다.
② 단어는 함께 등장하는 주변 단어를 통해 그 의미를 파악할 수 있다.
③ 모든 단어의 의미는 서로 독립적이다.
④ 단어 빈도는 의미와 전혀 관계가 없다.

### 8.

워드 임베딩 공간에서 의미적으로 유사한 단어들은 일반적으로 어떻게 나타나는가?

① 서로 가까운 위치에 나타난다.
② 항상 직교한다.
③ 반드시 같은 벡터가 된다.
④ 차원이 서로 달라진다.

### 9.

Word2Vec에 대한 설명으로 맞는 것은?

① 2013년 Google에서 개발된 대표적 워드 임베딩 기법이다.
② 이미지 분할을 위한 모델이다.
③ 순환 연결을 갖는 RNN의 한 종류이다.
④ Transformer의 decoder 구조이다.

### 10.

Word2Vec의 핵심 아이디어는?

① 각 단어와 주변 단어 간의 관계를 예측하면서 단어 표현을 학습한다.
② 문장 전체를 원-핫 벡터 하나로 만든다.
③ 단어의 철자만 이용해 의미를 결정한다.
④ 모든 단어의 벡터를 고정된 임의 값으로 둔다.

### 11.

Skip-gram 방식의 입력과 출력 관계로 맞는 것은?

① 주변 단어를 입력으로 받아 중심 단어를 예측한다.
② 중심 단어를 입력으로 받아 주변 단어를 예측한다.
③ 문장 전체를 입력으로 받아 문장 길이를 예측한다.
④ 이전 문장으로 다음 문장을 예측한다.

### 12.

CBOW 방식의 입력과 출력 관계로 맞는 것은?

① 중심 단어로 주변 단어들을 예측한다.
② 주변 단어들의 집합으로 중심 단어를 예측한다.
③ 한 문장의 감정을 예측한다.
④ 이전 hidden state로 다음 hidden state만 예측한다.

### 13.

Word2Vec에서 window size가 의미하는 것은?

① 임베딩 벡터의 차원
② 중심 단어 주변에서 문맥으로 고려할 단어의 범위
③ 학습률
④ 전체 어휘 수

### 14.

Skip-gram의 장점으로 제시된 것은?

① 학습 속도가 항상 CBOW보다 빠르다.
② 적은 데이터에서도 잘 동작하고 희귀 단어나 구 표현에 강하다.
③ 희귀 단어 표현에 약하다.
④ 주변 단어를 입력으로만 사용한다.

### 15.

CBOW의 장점으로 제시된 것은?

① 희귀 단어에 특히 강하다.
② 학습 속도가 빠르고 자주 나오는 단어에 강하다.
③ 중심 단어로 주변 단어를 예측한다.
④ 순서 정보를 완벽하게 보존한다.

### 16.

CBOW의 한계로 가장 적절한 것은?

① 학습 속도가 매우 느리다.
② 희귀 단어 표현에 약할 수 있다.
③ 단어 임베딩을 만들 수 없다.
④ 주변 단어를 사용할 수 없다.

## B. 순차적 데이터

### 17.

순차적 데이터의 정의로 가장 적절한 것은?

① 입력 순서와 데이터 간 관계가 중요하지 않은 데이터
② 데이터가 입력되는 순서와 그 순서를 통한 관계가 중요한 데이터
③ 항상 길이가 동일한 데이터
④ 이미지 데이터만을 의미한다.

### 18.

순차적 데이터의 예로 제시되지 않은 것은?

① 오디오
② 텍스트
③ 비디오
④ 무작위로 섞인 독립 표본만으로 구성된 표

### 19.

순차적 데이터에서 순서가 중요한 이유는?

① 순서가 바뀌면 의미나 관계가 달라질 수 있기 때문이다.
② 순서를 바꾸면 항상 데이터 크기가 줄어들기 때문이다.
③ 순서가 있어야 원-핫 인코딩이 가능하기 때문이다.
④ 순서가 모델의 파라미터 수를 0으로 만들기 때문이다.

### 20.

순차적 데이터의 장기 의존성(long-term dependency)에 대한 설명으로 맞는 것은?

① 멀리 떨어진 과거 정보가 현재 또는 미래의 판단에 영향을 줄 수 있다.
② 인접한 두 데이터 사이에는 관계가 없다.
③ 모든 데이터는 이전 정보와 독립이다.
④ 입력 길이가 항상 한 개라는 뜻이다.

### 21.

순차적 데이터의 가변 길이(variable length) 특성을 설명한 것은?

① 모든 문장은 동일한 단어 수를 가진다.
② 순차 데이터는 샘플마다 길이가 다를 수 있다.
③ 입력 차원은 반드시 1이다.
④ 출력은 항상 하나이다.

### 22.

순차적 데이터를 처리하기 위한 모델의 예로 제시된 조합은?

① RNN, LSTM, Transformer
② K-means, PCA, DBSCAN
③ CNN, SVM, KNN만
④ 선형 회귀만

## C. RNN

### 23.

RNN이 일반적인 MLP와 구별되는 핵심 특징은?

① 이전 시점의 정보를 hidden state를 통해 다음 시점으로 전달한다.
② 입력을 한 번만 처리한다.
③ 가중치를 전혀 사용하지 않는다.
④ 순서 정보를 제거한다.

### 24.

RNN에서 hidden state의 역할로 가장 적절한 것은?

① 이전까지 처리한 순차 정보의 요약을 유지한다.
② 단어 사전의 크기를 저장한다.
③ 손실함수를 제거한다.
④ 모든 출력을 원-핫 벡터로 만든다.

### 25.

RNN의 현재 hidden state (h_t)를 계산할 때 일반적으로 사용하는 정보는?

① 현재 입력 (x_t)와 이전 hidden state (h_{t-1})
② 현재 입력만
③ 다음 시점의 출력만
④ 전체 테스트 데이터의 정답

### 26.

기본 RNN의 hidden state를 나타내는 식으로 가장 적절한 것은?

① (h_t=tanh(W_hh h_{t-1}+W_xh x_t))
② (h_t=x_t-h_{t-1})만
③ (h_t=softmax(x_t))만
④ (h_t=W)만

### 27.

RNN 출력 (y_t)의 계산에 직접 활용될 수 있는 것은?

① 현재 hidden state (h_t)
② 단어 사전 크기만
③ window size만
④ 배치 번호만

### 28.

RNN의 many-to-one 구조가 적합한 예는?

① 문장 감정 분류
② 기계 번역
③ 각 단어마다 품사를 출력하는 태깅
④ 이미지의 모든 픽셀 복원

### 29.

RNN의 one-to-many 구조가 적합한 예로 볼 수 있는 것은?

① 하나의 입력으로 여러 시점의 출력을 생성하는 작업
② 문장 전체를 하나의 감정으로 분류
③ 입력과 출력이 모두 하나
④ 독립적인 표 데이터 회귀

### 30.

RNN의 many-to-many 구조가 적합한 예는?

① 기계 번역과 같이 입력과 출력이 모두 시퀀스인 작업
② 숫자 하나의 제곱 계산
③ 단일 이미지 분류
④ K-means 군집화

### 31.

기본 RNN의 중요한 문제로 강조된 것은?

① 장기 의존성 학습의 어려움
② 임베딩을 전혀 사용할 수 없음
③ 순차 데이터를 처리할 수 없음
④ hidden state가 존재하지 않음

### 32.

RNN에서 vanishing gradient가 발생하면 나타날 수 있는 현상은?

① 먼 과거 시점의 정보가 학습 과정에서 제대로 전달되지 않는다.
② 모든 gradient가 무조건 커진다.
③ 모델의 입력 길이가 자동으로 증가한다.
④ 출력 클래스 수가 증가한다.

### 33.

RNN에서 gradient가 여러 시점을 거치며 역전파되는 학습 방식과 가장 관련 깊은 것은?

① BPTT
② PCA
③ KNN
④ Bagging

### 34.

RNN이 순차 데이터 처리에 적합한 이유는?

① 동일한 recurrent 구조를 시간축으로 반복하며 이전 상태를 다음 계산에 활용하기 때문이다.
② 모든 시점의 입력을 독립적으로만 처리하기 때문이다.
③ 순서를 무작위로 섞기 때문이다.
④ hidden state를 사용하지 않기 때문이다.

## D. LSTM

### 35.

LSTM이 제안된 주된 목적은?

① RNN의 장기 의존성 문제를 완화하기 위해
② 워드 임베딩 차원을 무한대로 만들기 위해
③ 이미지 분류만 수행하기 위해
④ attention을 제거하기 위해

### 36.

LSTM이 기본 RNN과 비교해 추가로 유지하는 핵심 상태는?

① cell state
② vocabulary state
③ image state
④ batch state

### 37.

LSTM의 forget gate 역할은?

① 이전 cell state에서 무엇을 유지하고 무엇을 버릴지 결정한다.
② 최종 출력 클래스만 결정한다.
③ 입력 문장의 길이를 결정한다.
④ 임베딩 차원을 정한다.

### 38.

LSTM의 input gate 역할은?

① 새로운 정보 중 무엇을 cell state에 저장할지 조절한다.
② 과거 정보를 모두 무조건 삭제한다.
③ 출력만 생성한다.
④ 학습률을 정한다.

### 39.

LSTM의 output gate 역할은?

① cell state를 바탕으로 현재 hidden state로 내보낼 정보를 조절한다.
② 단어 사전을 생성한다.
③ 과거 cell state를 모두 삭제한다.
④ 입력 임베딩을 원-핫으로 변환한다.

### 40.

forget gate의 출력 범위를 0과 1 사이로 조절하는 데 일반적으로 사용되는 함수는?

① sigmoid
② ReLU만
③ argmax
④ k-means

### 41.

LSTM에서 새로운 후보 cell content를 만들 때 주로 사용하는 활성화 함수는?

① tanh
② softmax만
③ step function만
④ PCA

### 42.

LSTM의 새로운 cell state 계산 아이디어로 가장 적절한 것은?

① 이전 cell state 중 유지할 부분과 새로 추가할 정보를 결합한다.
② 이전 cell state를 항상 완전히 삭제한다.
③ hidden state와 무관하게 임의의 값으로 정한다.
④ 단어 빈도만 저장한다.

### 43.

다음 식이 나타내는 LSTM 구성 요소는?
(f_t=sigma(W_f[h_{t-1},x_t]+b_f))

① forget gate
② input gate
③ output gate
④ positional encoding

### 44.

다음 식의 의미로 가장 적절한 것은?
(C_t=f_t*C_{t-1}+i_t*tilde(C_t))

① 이전 기억을 선택적으로 유지하고 새로운 후보 정보를 선택적으로 더해 cell state를 갱신한다.
② 모든 기억을 삭제한다.
③ attention score를 계산한다.
④ 단어를 원-핫 벡터로 만든다.

## E. Self-Attention

### 45.

Self-Attention이 RNN의 순차 처리 방식과 비교해 갖는 중요한 특징은?

① 문장 내 여러 위치의 관계를 직접 계산할 수 있다.
② 이전 시점 정보만 사용할 수 있다.
③ 모든 토큰을 반드시 한 개씩만 순차 처리해야 한다.
④ 단어 간 관계를 계산하지 않는다.

### 46.

Attention에서 Query, Key, Value에 대한 설명으로 맞는 것은?

① Query와 Key의 관련도를 이용해 Value들을 가중합한다.
② Query만 사용하고 Key와 Value는 사용하지 않는다.
③ Key가 최종 정답 레이블이다.
④ Value는 항상 원-핫 벡터여야 한다.

### 47.

Self-Attention에서 Query, Key, Value는 일반적으로 어디에서 만들어지는가?

① 같은 입력 시퀀스의 표현으로부터 서로 다른 선형 변환을 통해 만들어진다.
② 테스트 정답으로부터 만들어진다.
③ 단어 사전의 크기로부터만 만들어진다.
④ 이미지 픽셀에서만 만들어진다.

### 48.

Attention score를 정규화하여 각 Value의 가중치를 얻는 데 주로 사용하는 함수는?

① softmax
② tanh만
③ ReLU만
④ sigmoid만

### 49.

Scaled Dot-Product Attention에서 dot product 값을 (sqrt(d_k))로 나누는 이유는?

① 차원이 커질 때 내적 값이 지나치게 커져 softmax가 극단적으로 되는 현상을 완화하기 위해
② 모델 파라미터를 제거하기 위해
③ 입력 순서를 무작위화하기 위해
④ 단어 수를 줄이기 위해

### 50.

Self-Attention만으로는 토큰의 순서를 자연스럽게 알기 어려운 이유는?

① attention 계산 자체는 위치 순서를 내장하지 않기 때문이다.
② Query가 존재하지 않기 때문이다.
③ Value가 존재하지 않기 때문이다.
④ 모든 단어가 원-핫이기 때문이다.

### 51.

Transformer에서 토큰 순서 정보를 제공하기 위해 사용하는 것은?

① Positional Encoding
② Negative Sampling
③ Dropout만
④ One-hot decoding

### 52.

positional encoding의 방법으로 제시된 것은?

① Sinusoidal Position Encoding과 Learned Absolute Position Embedding
② K-means와 PCA
③ CBOW와 Skip-gram
④ RNN과 LSTM

### 53.

Self-Attention 블록에 Feed-Forward Network를 추가하는 이유로 가장 적절한 것은?

① attention으로 관계를 반영한 각 위치의 표현에 비선형 변환 능력을 추가하기 위해
② 토큰 순서를 제거하기 위해
③ attention을 사용하지 않기 위해
④ 모든 파라미터를 공유하지 않기 위해

### 54.

Masked Self-Attention의 목적은?

① 현재 위치가 미래 토큰 정보를 미리 보지 못하도록 가린다.
② 과거 토큰을 모두 삭제한다.
③ 모든 attention score를 1로 만든다.
④ encoder에서만 미래 토큰을 보게 한다.

## F. Transformer

### 55.

Transformer가 처음 제안된 대표 논문은?

① Attention Is All You Need
② Word2Vec Is All You Need
③ ImageNet Classification
④ AlexNet Revisited

### 56.

Transformer의 기본 구조로 맞는 것은?

① encoder와 decoder
② CNN만
③ RNN만
④ KNN과 SVM

### 57.

Multi-Head Attention을 사용하는 주된 이유는?

① 서로 다른 표현 공간에서 다양한 관계를 동시에 학습하기 위해
② attention을 한 번도 계산하지 않기 위해
③ 모델의 모든 head가 반드시 같은 관계만 보게 하기 위해
④ 순서 정보를 제거하기 위해

### 58.

Residual Connection의 목적과 가장 가까운 것은?

① 깊은 네트워크에서 정보와 gradient의 흐름을 돕는다.
② 단어를 원-핫으로 바꾼다.
③ vocabulary를 줄인다.
④ masking을 제거한다.

### 59.

Layer Normalization의 역할로 가장 적절한 것은?

① 각 층의 표현을 정규화하여 학습 안정성을 높이는 데 도움을 준다.
② 문장 길이를 모두 같게 만든다.
③ 모든 attention head를 제거한다.
④ gradient 계산을 중단한다.

### 60.

Transformer decoder의 첫 번째 attention에서 사용하는 핵심 방식은?

① Masked Multi-Head Self-Attention
② CBOW
③ K-means
④ 단순 선형회귀

### 61.

Encoder-Decoder Transformer에서 Cross-Attention의 역할은?

① decoder의 Query가 encoder 출력의 Key와 Value를 참조하도록 한다.
② encoder와 decoder를 완전히 독립적으로 만든다.
③ 미래 토큰을 정답으로 직접 제공한다.
④ 단어 임베딩을 제거한다.

### 62.

Transformer가 RNN에 비해 학습 병렬화에 유리한 이유는?

① self-attention은 여러 토큰 위치를 동시에 계산할 수 있기 때문이다.
② hidden state를 한 시점씩 반드시 전달해야 하기 때문이다.
③ 모든 입력 길이가 1이기 때문이다.
④ recurrent connection이 더 많기 때문이다.

## G. 사전학습 기반 언어모델

### 63.

사전학습(pretraining)의 핵심 아이디어는?

① 대규모 데이터로 일반적인 언어 능력을 먼저 학습한 뒤 다양한 downstream task에 활용한다.
② 각 태스크마다 항상 무작위 초기화한다.
③ 학습 데이터를 사용하지 않는다.
④ 모델 가중치를 고정된 0으로 둔다.

### 64.

BERT의 기본 구조는 무엇인가?

① Transformer Encoder
② Transformer Decoder만
③ LSTM Decoder
④ CNN Encoder만

### 65.

BERT의 대표적 사전학습 과제 중 Masked Language Model(MLM)은 무엇을 하는가?

① 입력의 일부 토큰을 가리고 원래 토큰을 예측한다.
② 다음 이미지를 생성한다.
③ 전체 문장을 삭제한다.
④ 단어 순서를 무작위로 정답으로 사용한다.

### 66.

BERT의 Next Sentence Prediction(NSP)의 기본 아이디어는?

① 두 문장이 실제로 이어지는 관계인지 예측한다.
② 한 단어의 품사만 예측한다.
③ 이미지와 텍스트의 유사도를 계산한다.
④ 다음 글자 하나만 예측한다.

### 67.

Encoder 기반 모델이 특히 적합한 작업은?

① 문장 분류, 자연어 추론, 질의응답, 토큰 분류 등 입력 이해 중심 작업
② 왼쪽에서 오른쪽으로 장문을 생성하는 작업만
③ 이미지 생성만
④ 음성 합성만

### 68.

T5의 구조와 학습 관점으로 가장 적절한 것은?

① Encoder-Decoder 구조를 사용하며 다양한 NLP 문제를 Text-to-Text 형태로 통일한다.
② Encoder만 사용하며 MLM만 수행한다.
③ Decoder만 사용하며 이미지 생성만 한다.
④ RNN만 사용한다.

### 69.

GPT 계열의 기본 구조로 가장 적절한 것은?

① Transformer Decoder 기반 autoregressive language model
② Transformer Encoder만 사용하는 bidirectional classifier
③ Word2Vec 기반 군집 모델
④ CNN 기반 객체 탐지 모델

### 70.

GPT의 Next Token Prediction 학습에서 모델이 수행하는 것은?

① 이전까지의 토큰을 바탕으로 다음 토큰을 예측한다.
② 미래의 모든 정답 토큰을 동시에 입력으로 본다.
③ 단어의 위치를 제거한다.
④ 두 문장이 이어지는지만 예측한다.

# PART 2. 단답형 20문제

## A. 워드 임베딩과 순차 모델

### 71.

한 원소만 1이고 나머지가 0인 벡터로 단어를 표현하는 방법을 무엇이라고 하는가?

### 72.

단어를 의미적 관계를 반영할 수 있는 밀집 연속 벡터로 표현하는 방법을 무엇이라고 하는가?

### 73.

중심 단어로 주변 단어를 예측하는 Word2Vec 알고리즘을 무엇이라고 하는가?

### 74.

주변 단어들로 중심 단어를 예측하는 Word2Vec 알고리즘을 무엇이라고 하는가?

### 75.

중심 단어 주변 몇 개 단어까지 문맥으로 사용할지를 나타내는 값을 무엇이라고 하는가?

### 76.

이전 시점의 정보를 hidden state에 담아 다음 시점으로 전달하는 순환 신경망을 무엇이라고 하는가?

### 77.

RNN에서 먼 과거의 정보가 학습 과정에서 점차 사라지는 대표적 문제를 무엇이라고 하는가?

### 78.

RNN의 장기 의존성 문제를 완화하기 위해 cell state와 gate 구조를 도입한 모델은 무엇인가?

### 79.

LSTM에서 이전 cell state의 정보를 얼마나 유지할지 결정하는 gate는 무엇인가?

### 80.

LSTM에서 새 정보를 cell state에 얼마나 기록할지 결정하는 gate는 무엇인가?

### 81.

LSTM에서 cell state를 바탕으로 현재 hidden state에 얼마나 출력할지 결정하는 gate는 무엇인가?

## B. Attention과 Transformer

### 82.

Attention에서 현재 토큰이 무엇을 찾고 있는지를 나타내는 벡터를 무엇이라고 하는가?

### 83.

Attention에서 Query와의 관련도를 비교하기 위한 벡터를 무엇이라고 하는가?

### 84.

Attention 가중치를 적용하여 최종적으로 합산되는 정보 벡터를 무엇이라고 하는가?

### 85.

Transformer에서 토큰의 순서 정보를 추가하기 위해 사용하는 기법을 무엇이라고 하는가?

### 86.

여러 개의 attention head를 병렬로 사용하여 다양한 관계를 학습하는 구조를 무엇이라고 하는가?

### 87.

Decoder가 미래 토큰을 미리 보지 못하게 하는 attention 방식을 무엇이라고 하는가?

### 88.

BERT의 대표적인 사전학습 방식으로 일부 토큰을 가린 뒤 원래 토큰을 맞히는 과제를 무엇이라고 하는가?

### 89.

다양한 NLP 문제를 입력 텍스트에서 출력 텍스트로 변환하는 하나의 형식으로 통일한 Encoder-Decoder 모델은 무엇인가?

### 90.

GPT-3에서 모델의 파라미터를 업데이트하지 않고 프롬프트 안의 예시를 이용해 새로운 태스크를 수행하는 현상을 무엇이라고 하는가?

# PART 3. 서술형 10문제

### 91. ★

원-핫 인코딩과 워드 임베딩의 차이를 벡터 차원, 희소성, 의미적 유사성 표현의 관점에서 비교하여 설명하시오.

### 92. ★

Word2Vec의 Skip-gram과 CBOW를 입력과 출력, 학습 속도, 희귀 단어 표현의 관점에서 비교하여 설명하시오.

### 93. ★

순차적 데이터의 세 가지 주요 특징인 순서의 중요성, 장기 의존성, 가변 길이를 각각 설명하고 자연어 처리에서 왜 중요한지 서술하시오.

### 94. ★★

기본 RNN에서 hidden state가 어떻게 계산되고 문맥 정보를 어떻게 전달하는지 설명한 뒤, 장기 의존성과 vanishing gradient 문제가 발생하는 이유를 서술하시오.

### 95. ★★

LSTM의 cell state, forget gate, input gate, output gate의 역할을 설명하고 LSTM이 기본 RNN보다 장기 의존성을 더 잘 다룰 수 있는 이유를 서술하시오.

### 96. ★★

Self-Attention에서 Query, Key, Value가 각각 어떤 역할을 하는지 설명하고 attention score, softmax, Value의 가중합을 통해 출력이 생성되는 과정을 서술하시오.

### 97. ★★

Transformer에서 Positional Encoding, Multi-Head Attention, Feed-Forward Network, Residual Connection, Layer Normalization이 각각 필요한 이유를 설명하시오.

### 98. ★★

Transformer Encoder와 Decoder의 구조 차이를 설명하고, Decoder의 Masked Self-Attention과 Encoder-Decoder Cross-Attention이 각각 어떤 역할을 하는지 서술하시오.

### 99. ★★

BERT, T5, GPT를 Encoder 모델, Encoder-Decoder 모델, Decoder 모델이라는 구조적 관점과 대표 학습 목표 및 적합한 작업의 관점에서 비교하시오.

### 100. ★★★

사전학습 기반 언어모델에서 pretraining과 downstream adaptation의 관계를 설명하고, GPT-3에서 나타난 In-Context Learning과 zero-shot, one-shot, few-shot prompting, Chain-of-Thought prompting의 의미를 함께 설명하시오.
