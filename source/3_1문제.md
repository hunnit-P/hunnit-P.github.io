# PART 1. 객관식 75문제

## A. CNN과 FCN 기초

### 1. 32×32 RGB 이미지를 FCN에 입력하기 위해 Flatten할 때 입력 벡터의 원소 수는?
① 1024
② 2048
③ 3072
④ 4096

### 2. FCN이 이미지 처리에서 CNN보다 불리한 가장 직접적인 이유는?
① 입력의 공간 구조를 보존하면서 지역 특징을 추출하기 때문이다.
② 이미지를 1차원으로 펼치므로 공간적 구조를 직접 활용하기 어렵고 파라미터 수가 커질 수 있기 때문이다.
③ 필터를 모든 위치에서 공유하기 때문이다.
④ 출력 채널 수를 조절할 수 없기 때문이다.

### 3. 합성곱 레이어의 핵심 역할로 가장 적절한 것은?
① 입력 이미지를 무조건 1차원으로 변환한다.
② 필터를 이동시키며 지역 영역과 연산해 특징 맵을 추출한다.
③ 클래스 확률만 계산한다.
④ 학습 가능한 파라미터 없이 최대값만 선택한다.

### 4. 입력이 3×32×32이고 필터 하나의 크기가 3×5×5일 때, 필터가 한 위치에서 사용하는 가중치 수는?
① 25
② 50
③ 75
④ 150

### 5. 합성곱 필터의 깊이(입력 채널 방향)에 대한 설명으로 옳은 것은?
① 항상 1이다.
② 입력 이미지의 높이와 같아야 한다.
③ 입력의 채널 수와 같아야 한다.
④ 출력 채널 수와 같아야 한다.

### 6. 입력 배치가 (N×C_in×H×W), 필터가 (C_out×C_in×K_h×K_w)일 때 출력의 채널 수는?
① N
② C_in
③ C_out
④ K_h×K_w

### 7. 32×32 입력에 5×5 필터, stride=1, padding=0을 적용하면 출력의 공간 크기는?
① 26×26
② 27×27
③ 28×28
④ 32×32

### 8. 32×32 입력에 3×3 필터, stride=1, padding=0을 적용하면 출력의 공간 크기는?
① 28×28
② 30×30
③ 31×31
④ 32×32

### 9. 32×32×3 입력에 5×5 필터 6개를 stride=1, padding=0으로 적용했을 때 출력 크기는?
① 32×32×6
② 28×28×6
③ 28×28×3
④ 6×6×28

### 10. 배치 크기 N=2, 입력 (2×3×32×32)에 5×5 필터 6개를 stride=1, padding=0으로 적용하면 출력 배치의 크기는?
① 2×6×28×28
② 6×2×28×28
③ 2×3×28×28
④ 2×6×32×32

## B. CNN 구조, 풀링, 수용영역

### 11. CNN에서 여러 합성곱 층 사이에 ReLU 같은 비선형 활성화 함수를 넣는 주된 이유는?
① 여러 선형 연산을 하나의 선형 연산으로 합칠 수 있게 하기 위해
② 모델에 비선형성을 부여하여 복잡한 패턴을 학습하게 하기 위해
③ 파라미터를 모두 0으로 만들기 위해
④ 출력 채널을 항상 1로 만들기 위해

### 12. 수용영역(Receptive Field)의 의미로 가장 적절한 것은?
① 모델의 전체 파라미터 수
② 한 출력 뉴런이 입력에서 영향을 받을 수 있는 영역
③ 한 배치에 포함되는 이미지 수
④ 학습률이 적용되는 범위

### 13. 네트워크가 깊어질수록 일반적으로 수용영역은 어떻게 되는가?
① 작아진다.
② 넓어진다.
③ 항상 1×1이다.
④ 채널 수와 동일해진다.

### 14. 수용영역을 확장하는 방법으로 보기 어려운 것은?
① 합성곱 레이어를 깊게 쌓기
② Pooling 사용
③ Stride 사용
④ 모든 레이어를 제거하기

### 15. 2×2 Max Pooling의 동작으로 옳은 것은?
① 각 2×2 영역의 평균을 출력한다.
② 각 2×2 영역의 최댓값을 출력한다.
③ 각 2×2 영역의 최솟값을 출력한다.
④ 2×2 영역을 그대로 복사한다.

### 16. Max Pooling의 특징으로 옳은 것은?
① 반드시 학습 가능한 가중치를 가진다.
② 공간 해상도를 줄이고 위치 변화에 대한 강건성을 높이는 데 도움을 준다.
③ 채널 수를 반드시 절반으로 줄인다.
④ 항상 출력 해상도를 증가시킨다.

### 17. 32×32×8 특징 맵에 2×2 Max Pooling, stride=2를 적용하면 출력 크기는?
① 32×32×8
② 16×16×8
③ 16×16×4
④ 8×8×8

### 18. 다음 4×4 단일 채널 입력에 2×2 Max Pooling, stride=2를 적용한 결과로 옳은 것은? 입력은 위에서부터 [1,1,2,4], [5,6,7,8], [3,2,1,0], [1,2,3,4]이다.
① [[6,8],[3,4]]
② [[1,2],[1,0]]
③ [[13,21],[8,8]]
④ [[3,4],[6,8]]

### 19. Strided Convolution의 장점으로 강의자료에서 강조된 것은?
① Pooling과 달리 커널을 학습하면서 해상도를 줄일 수 있다.
② 항상 파라미터가 0개이다.
③ 출력 해상도를 반드시 증가시킨다.
④ FC 레이어만 사용할 수 있게 한다.

### 20. 고도화된 CNN에서 Pooling 대신 Strided Convolution이 사용되는 이유로 가장 적절한 것은?
① 다운샘플링과 특징 추출을 학습 가능한 하나의 연산으로 결합할 수 있기 때문이다.
② 채널을 없애기 때문이다.
③ 비선형성을 제거하기 때문이다.
④ 모든 연산량을 0으로 만들기 때문이다.

## C. AlexNet과 계산 문제

### 21. AlexNet의 입력 이미지 크기로 강의자료에서 사용된 것은?
① 32×32×3
② 128×128×3
③ 224×224×3
④ 227×227×3

### 22. AlexNet Conv1이 227×227 입력에 11×11 커널, stride=4, padding=0을 적용할 때 공간 출력 크기는?
① 27×27
② 55×55
③ 56×56
④ 96×96

### 23. AlexNet Conv1의 필터 수가 96개라면 출력 크기는?
① 55×55×96
② 96×96×55
③ 27×27×96
④ 55×55×3

### 24. AlexNet Conv1의 파라미터 수를 bias까지 포함해 계산한 값은?
① 3,072
② 11,616
③ 34,944
④ 105,415,200

### 25. AlexNet Conv1 출력 55×55×96을 float32로 저장한다고 할 때 필요한 메모리는 약 몇 KB인가? (1 float=4 byte, 1KB≈1024 byte)
① 284 KB
② 567 KB
③ 1,134 KB
④ 4,536 KB

### 26. AlexNet Conv1의 FLOPs를 강의자료 방식대로 곱셈과 덧셈을 각각 1회로 계산하면 약 얼마인가?
① 34.9K
② 37.7M
③ 105.4M
④ 447.9M

### 27. AlexNet에서 메모리 사용량이 특히 큰 경향을 보이는 부분은?
① 초기 합성곱 레이어
② 마지막 Softmax만
③ Pooling 레이어만
④ bias만

### 28. AlexNet에서 파라미터 수가 특히 집중되는 부분은?
① Max Pooling
② Fully Connected 레이어
③ 입력 이미지
④ ReLU

### 29. AlexNet에서 FLOPs가 주로 발생하는 부분은?
① 합성곱 레이어
② Flatten만
③ Softmax만
④ Max Pooling만

### 30. AlexNet에서 Conv2/Conv4/Conv5에 그룹 합성곱을 적용했던 역사적 이유로 강의자료에서 설명한 것은?
① 두 GPU에 병렬로 나누어 연산하기 위해
② 데이터 증강을 위해
③ 클래스 수를 늘리기 위해
④ ReLU를 제거하기 위해

### 31. AlexNet의 세 번째 Max Pooling 이후 출력이 6×6×256이라면 Flatten된 원소 수는?
① 1536
② 4096
③ 9216
④ 16384

### 32. AlexNet에서 FC1의 입력 9216, 출력 4096일 때 bias를 무시한 가중치 수는?
① 4,096
② 9,216
③ 16,777,216
④ 37,748,736

### 33. float32에서 원소 하나가 차지하는 바이트 수는?
① 1
② 2
③ 4
④ 8

### 34. ★ CNN 표에서 Conv1 출력 원소 수가 55×55×96일 때 원소 수는?
① 221,184
② 290,400
③ 307,200
④ 921,600

### 35. ★ 어떤 FC 레이어의 출력이 4096이고 배치 크기가 5일 때 출력 원소 수는?
① 4,096
② 8,192
③ 20,480
④ 221,184

## D. VGGNet, ResNet, MobileNet

### 36. VGGNet의 대표적인 설계 특징은?
① 매우 큰 커널 하나만 사용한다.
② 작은 3×3 합성곱을 반복적으로 쌓는다.
③ 합성곱을 전혀 사용하지 않는다.
④ RNN 셀만 사용한다.

### 37. VGG-16에 대한 설명으로 옳은 것은?
① 합성곱 블록과 Max Pooling을 반복하고 뒤에 FC 레이어를 둔다.
② Skip connection이 핵심이다.
③ Depthwise separable convolution이 핵심이다.
④ 순차 데이터 전용 모델이다.

### 38. VGG-16과 VGG-19의 이름에서 숫자가 주로 나타내는 것은?
① 입력 채널 수
② 학습 가능한 레이어 수
③ 클래스 수
④ 배치 크기

### 39. 깊은 네트워크에서 발생할 수 있는 대표적 문제는?
① 기울기 소실 또는 폭발
② 입력 이미지가 자동으로 커짐
③ 클래스 수가 0이 됨
④ 파라미터가 자동으로 삭제됨

### 40. ResNet의 핵심 아이디어는?
① 모든 층을 Fully Connected로 바꾸는 것
② Skip connection을 통해 입력을 잔차 블록의 출력에 더하는 것
③ 모든 활성화 함수를 제거하는 것
④ 채널을 항상 1개로 만드는 것

### 41. 기본적인 ResNet 잔차 블록의 출력 형태로 가장 적절한 것은?
① F(x)-x
② F(x)+x
③ F(x)×x
④ F(x)/x

### 42. ResNet의 Skip connection이 학습에 주는 핵심 이점은?
① 깊은 층에서도 정보와 기울기의 전달 경로를 제공한다.
② 파라미터를 반드시 0개로 만든다.
③ 입력을 무조건 1차원으로 만든다.
④ 배치 크기를 자동으로 1로 만든다.

### 43. 강의자료 기준 ResNet-34와 ResNet-50의 주요 구조 차이는?
① ResNet-34는 Basic block, ResNet-50은 Bottleneck block을 사용한다.
② ResNet-34만 합성곱을 사용한다.
③ ResNet-50만 ReLU를 사용한다.
④ ResNet-34는 RNN이고 ResNet-50은 CNN이다.

### 44. ResNet 학습 레시피로 강의자료에 포함된 것은?
① SGD + momentum 0.9
② Dropout 0.9 필수
③ 배치 크기 1 고정
④ 학습률 10 고정

### 45. MobileNet의 목표로 가장 적절한 것은?
① 모바일/임베디드 환경에서도 효율적으로 동작하는 CNN
② 초대형 서버에서만 동작하는 RNN
③ 텍스트 전용 생성 모델
④ 데이터 전처리 전용 알고리즘

### 46. MobileNet의 핵심 연산인 Depthwise Separable Convolution의 구성은?
① Depthwise convolution + Pointwise 1×1 convolution
② Max Pooling + Average Pooling
③ RNN + LSTM
④ FC + Softmax만

### 47. Depthwise Convolution의 설명으로 옳은 것은?
① 각 입력 채널별로 독립적인 공간 합성곱을 수행한다.
② 모든 채널을 하나로 합쳐 FC 연산을 한다.
③ 공간 차원을 전혀 처리하지 않는다.
④ 출력 클래스만 계산한다.

### 48. Pointwise Convolution의 설명으로 옳은 것은?
① 1×1 합성곱으로 채널 방향 정보를 결합한다.
② 11×11 합성곱으로만 동작한다.
③ 각 채널을 완전히 독립적으로 유지한다.
④ 순차 데이터의 은닉 상태를 계산한다.

## E. RNN과 LSTM

### 49. RNN이 주로 다루는 데이터 유형은?
① 순차적 데이터
② 순서가 전혀 없는 상수 데이터만
③ 오직 정지 영상만
④ 오직 표 형태 데이터만

### 50. RNN의 은닉 상태 h_t의 역할로 가장 적절한 것은?
① 현재 입력과 이전 상태의 정보를 반영한 내부 메모리 역할을 한다.
② 항상 입력과 동일한 값이다.
③ 학습이 끝날 때까지 0으로 고정된다.
④ 오직 이미지의 픽셀 수만 저장한다.

### 51. RNN의 입력 레이어에서 문자를 다루는 방식으로 강의자료에 제시된 것은?
① 문자를 One-hot vector로 변환한다.
② 모든 문자를 동일한 숫자로 바꾼다.
③ 문자를 이미지로만 변환한다.
④ 입력을 삭제한다.

### 52. RNN에서 현재 은닉 상태를 계산할 때 사용되는 정보는?
① 현재 입력과 이전 은닉 상태
② 다음 시점의 정답만
③ 현재 출력만
④ 배치 크기만

### 53. RNN의 출력 레이어는 일반적으로 무엇을 입력으로 사용해 출력을 계산하는가?
① 은닉 상태
② 원본 이미지의 높이만
③ 학습률
④ epoch 번호

### 54. ★ RNN에서 은닉 상태 h_t에 대한 설명으로 옳은 것은?
① h_t는 매 시점 완전히 독립적이므로 이전 정보가 반영되지 않는다.
② tanh는 선형 함수이므로 RNN에 비선형성을 주지 못한다.
③ 마지막 또는 각 시점의 h_t를 출력 계산에 활용할 수 있으며, h_t는 이전 상태의 문맥을 전달한다.
④ h_t는 정답 레이블 그 자체이다.

### 55. 기본 RNN에서 긴 시퀀스를 학습할 때 발생할 수 있는 문제는?
① 기울기 소실 또는 폭발
② 출력 채널이 무조건 3이 됨
③ 이미지 해상도가 자동으로 증가함
④ 파라미터가 전혀 학습되지 않음

### 56. RNN의 가중치가 시간축을 따라 반복적으로 사용되는 특성과 관련된 설명으로 옳은 것은?
① 같은 전이 가중치가 여러 시점에 공유될 수 있다.
② 시점마다 반드시 완전히 다른 모델을 새로 학습한다.
③ 시간축에서는 역전파를 하지 않는다.
④ 은닉 상태는 다음 시점으로 전달되지 않는다.

### 57. LSTM이 기본 RNN보다 장기 의존성 학습에 유리한 이유는?
① Cell state와 게이트 구조로 정보의 유지와 제거를 조절하기 때문이다.
② 모든 가중치를 0으로 만들기 때문이다.
③ 순차성을 제거하기 때문이다.
④ 합성곱만 사용하기 때문이다.

### 58. LSTM의 Forget gate 역할은?
① 이전 cell state의 어떤 정보를 잊을지 조절한다.
② 최종 클래스 수를 결정한다.
③ 이미지 크기를 줄인다.
④ 학습률을 정한다.

### 59. LSTM의 Input gate 역할은?
① 새로운 정보 중 cell state에 얼마나 반영할지 조절한다.
② 이전 정보를 전부 삭제한다.
③ 출력 클래스만 선택한다.
④ Pooling을 수행한다.

### 60. LSTM의 Output gate 역할은?
① cell state를 바탕으로 현재 hidden state로 얼마나 내보낼지 조절한다.
② 학습 데이터를 섞는다.
③ 입력 채널 수를 3으로 고정한다.
④ 정규화 계수를 결정한다.

## F. Attention과 ViT

### 61. Attention의 핵심 아이디어로 가장 적절한 것은?
① 모든 입력 위치를 항상 동일하게 취급한다.
② 현재 처리에 중요한 입력 위치에 더 큰 가중치를 부여한다.
③ 입력 순서를 무조건 삭제한다.
④ 합성곱 커널만 크게 만든다.

### 62. Transformer 계열에서 Attention을 사용하는 중요한 이유는?
① 입력 요소 간 관계를 직접 모델링할 수 있기 때문이다.
② 반드시 RNN의 순차 계산만 사용하기 때문이다.
③ 이미지 해상도를 무조건 절반으로 줄이기 때문이다.
④ 파라미터를 0으로 만들기 때문이다.

### 63. ViT에서 이미지를 Transformer에 넣기 위해 가장 먼저 하는 핵심 처리로 옳은 것은?
① 이미지를 패치로 나눈 뒤 각 패치를 토큰처럼 임베딩한다.
② 이미지를 한 픽셀만 남긴다.
③ 모든 픽셀을 삭제한다.
④ Max Pooling만 반복한다.

### 64. ViT의 패치 위치 인코딩이 필요한 이유는?
① 패치의 공간적 위치 정보를 모델에 제공하기 위해
② 채널 수를 0으로 만들기 위해
③ 학습률을 계산하기 위해
④ 손실함수를 제거하기 위해

### 65. ViT 인코더의 대표 구성으로 옳은 것은?
① 정규화, Multi-head Attention, MLP, 잔차 연결
② Max Pooling만 반복
③ LSTM gate만 반복
④ FC 한 층만 사용

### 66. ViT에서 CNN과 달리 기대하는 장점으로 강의자료에서 강조된 것은?
① 수용영역에 제한되지 않고 전역적 맥락을 이해할 수 있다.
② 이미지의 지역 정보만 볼 수 있다.
③ 위치 정보가 전혀 필요 없다.
④ 데이터가 적을수록 항상 CNN보다 우수하다.

### 67. ViT가 작은 데이터셋에서 불리할 수 있는 이유로 가장 적절한 것은?
① CNN의 지역성 같은 강한 귀납적 편향이 상대적으로 약해 많은 데이터가 필요할 수 있기 때문이다.
② 파라미터를 학습할 수 없기 때문이다.
③ 이미지를 입력받을 수 없기 때문이다.
④ Attention을 사용할 수 없기 때문이다.

## G. 활성화, 초기화, 정규화, 학습 전략

### 68. 활성화 함수의 가장 중요한 역할은?
① 신경망에 비선형성을 부여한다.
② 모든 가중치를 동일하게 만든다.
③ 입력 차원을 반드시 1로 만든다.
④ 데이터셋을 자동 수집한다.

### 69. ReLU 함수로 옳은 것은?
① f(x)=max(0,x)
② f(x)=x^2
③ f(x)=1/x
④ f(x)=|x|+1

### 70. Leaky ReLU가 ReLU와 다른 점은?
① 음수 영역에도 작은 기울기를 둔다.
② 양수 영역을 모두 0으로 만든다.
③ 출력이 항상 0과 1 사이이다.
④ 선형 함수만 사용한다.

### 71. ELU의 장점으로 강의자료에서 언급된 것은?
① 음수 영역의 포화 구간을 통해 평균 출력을 0 근처로 유도하고 음수 영역의 기울기를 완전히 없애지 않는다.
② 계산 복잡성이 ReLU보다 항상 낮다.
③ 하이퍼파라미터가 전혀 없다.
④ 양수 입력을 모두 0으로 만든다.

### 72. ReLU를 사용하는 깊은 네트워크의 초기화로 적합한 것은?
① He 초기화
② 모든 가중치 0 초기화
③ 모든 가중치 100 초기화
④ 임의로 동일 상수 초기화

### 73. L1 정규화의 특징으로 옳은 것은?
① 일부 가중치를 정확히 0으로 만들어 희소성을 유도할 수 있다.
② 모든 가중치를 동일한 값으로 만든다.
③ 큰 가중치에 제곱 패널티만 준다.
④ 학습률을 증가시키는 방법이다.

### 74. L2 정규화(Weight Decay)의 특징으로 옳은 것은?
① 가중치 크기의 제곱에 비례한 패널티를 주어 지나치게 큰 가중치를 억제한다.
② 모든 가중치를 즉시 0으로 만든다.
③ 입력 이미지를 증강한다.
④ 은닉 상태를 초기화한다.

### 75. Early Stopping의 기준으로 가장 적절한 것은?
① 훈련 성능은 계속 좋아져도 검증 성능이 더 이상 좋아지지 않고 악화되기 시작할 때 학습을 멈춘다.
② 첫 epoch 전에 항상 멈춘다.
③ 훈련 손실이 감소하면 즉시 멈춘다.
④ 검증 데이터를 사용하지 않는다.

# PART 2. 단답형 15문제

## A. CNN 계산과 모델 구조

### 76. 합성곱 출력 공간 크기를 입력 크기 H, 커널 K, 패딩 P, 스트라이드 S를 사용해 한 변 기준으로 나타내는 공식을 쓰시오.

### 77. 입력 32×32×3에 5×5 필터 6개, stride=1, padding=0을 적용할 때 출력 텐서의 크기를 쓰시오.

### 78. 32×32×8 특징 맵에 2×2 Max Pooling, stride=2를 적용할 때 출력 크기를 쓰시오.

### 79. AlexNet Conv1에서 입력 227×227×3, 커널 11×11, 필터 96개, stride=4, padding=0일 때 출력 크기를 쓰시오.

### 80. AlexNet Conv1의 파라미터 수를 bias 포함하여 계산하시오.

### 81. AlexNet의 마지막 Pooling 출력 6×6×256을 Flatten했을 때 벡터 길이를 쓰시오.

### 82. VGGNet이 큰 커널 대신 반복적으로 사용하는 대표적인 커널 크기를 쓰시오.

### 83. ResNet의 잔차 블록에서 입력 x와 잔차 함수 F(x)를 결합한 출력식을 쓰시오.

### 84. MobileNet에서 공간 방향 합성곱과 채널 결합을 분리한 연산의 이름을 쓰시오.

## B. RNN, ViT, 학습 전략

### 85. RNN에서 이전 시점의 정보를 다음 시점으로 전달하는 내부 상태의 이름과 기호를 쓰시오.

### 86. LSTM에서 장기 정보를 전달하는 상태의 이름과 기호를 쓰시오.

### 87. LSTM의 세 가지 대표 게이트 이름을 쓰시오.

### 88. ViT에서 이미지를 작은 조각으로 나눈 뒤 토큰처럼 사용하는 그 조각을 무엇이라고 하는가?

### 89. ReLU 사용 시 입력 차원 D_in에 대해 He 초기화의 표준편차를 강의자료 표기대로 쓰시오.

### 90. 초기 학습률 0.1을 사용하고 30 epoch마다 학습률을 1/10로 줄이는 계단식 스케줄에서 0~29, 30~59, 60~89 epoch의 학습률을 순서대로 쓰시오.

# PART 3. 서술형 30문제

### 91. FCN과 CNN이 이미지를 처리하는 방식을 공간 구조 보존, 파라미터 공유, 지역 특징 추출 관점에서 비교하여 설명하시오.

### 92. 합성곱 레이어에서 입력 채널 수, 출력 채널 수, 커널 크기, stride, padding이 출력 텐서의 크기와 파라미터 수에 어떤 영향을 주는지 설명하시오.

### 93. Pooling과 Strided Convolution을 비교하고, 고도화된 CNN에서 Strided Convolution이 Pooling을 대체할 수 있는 이유를 설명하시오.

### 94. AlexNet에서 메모리 사용량은 초기 레이어에, 파라미터 수는 FC 레이어에, FLOPs는 합성곱 레이어에 집중되는 이유를 각각 설명하시오.

### 95. AlexNet, VGGNet, ResNet, MobileNet의 핵심 설계 아이디어를 각각 한 가지 이상 제시하고 CNN 구조가 어떤 방향으로 발전했는지 설명하시오.

### 96. 기본 RNN의 순차 처리 과정에서 입력 x_t, 이전 은닉 상태 h_(t-1), 현재 은닉 상태 h_t, 출력 y_t가 어떤 관계를 갖는지 설명하고 긴 시퀀스에서 발생하는 기울기 소실/폭발 문제도 함께 설명하시오.

### 97. LSTM의 cell state와 Forget/Input/Output gate가 각각 어떤 역할을 하며, 이것이 기본 RNN의 장기 의존성 문제를 어떻게 완화하는지 설명하시오.

### 98. Attention의 기본 아이디어와 Transformer가 RNN 기반 순차 처리와 다른 점을 설명하고, ViT가 이미지를 패치 토큰으로 처리하는 흐름을 설명하시오.

### 99. Sigmoid, tanh, ReLU, Leaky ReLU, ELU를 출력 범위 또는 음수 영역의 처리, 기울기 소실 가능성, 계산 특성 관점에서 비교하고 일반적인 시작점으로 ReLU가 권장되는 이유를 설명하시오.

### 100. 과적합을 줄이기 위한 L1/L2 정규화, 학습률 스케줄링, Early Stopping의 목적과 동작을 각각 설명하시오.


### 101. ★ 입력 크기가 64×64×3인 이미지에 3×3 필터 8개, stride=1, padding=1인 합성곱을 적용한 뒤 2×2 Max Pooling, stride=2를 적용한다. 합성곱 직후와 Pooling 직후의 출력 크기를 각각 계산하고, padding=1이 공간 크기에 미치는 영향을 설명하시오.

### 102. ★★ 입력 크기가 32×32×8인 특징 맵에 3×3 필터 16개, stride=2, padding=1인 합성곱을 적용한다. 출력 텐서의 크기와 bias를 포함한 학습 파라미터 수를 계산하시오.

### 103. ★★ 32×32×8 특징 맵과 16×16×8 특징 맵의 원소 수를 각각 구하고, 두 특징 맵을 float32로 저장할 때 필요한 메모리의 비율을 설명하시오.

### 104. ★★ 어떤 CNN의 Conv 출력이 24×24×32이고 배치 크기가 12이다. 이 출력 전체를 float32로 저장할 때 필요한 바이트 수를 계산하고, 배치 크기가 activation memory에 어떤 영향을 주는지 설명하시오.

### 105. ★★★ 입력 227×227×3에 AlexNet Conv1의 11×11 필터 96개, stride=4, padding=0을 적용한다. 출력 공간 크기, 출력 원소 수, bias 포함 파라미터 수를 순서대로 계산하시오.

### 106. ★★★ AlexNet의 6×6×256 특징 맵을 Flatten한 뒤 4096개의 뉴런을 가진 FC 레이어에 연결한다. Flatten 길이와 bias를 포함한 FC 레이어의 파라미터 수를 계산하고, 이 값이 큰 이유를 설명하시오.

### 107. ★★ 합성곱 레이어와 Fully Connected 레이어가 같은 수의 출력 뉴런을 만든다고 가정할 때, CNN의 가중치 공유가 파라미터 수를 줄이는 원리를 구체적으로 설명하시오.

### 108. ★★ 5×5 합성곱 한 층을 사용하는 경우와 3×3 합성곱을 연속으로 두 층 사용하는 경우를 수용영역, 비선형성, 파라미터 효율 관점에서 비교하시오.

### 109. ★★ VGGNet이 작은 3×3 필터를 반복해 깊은 구조를 만든 이유를 설명하고, 이러한 설계가 큰 필터 하나를 사용하는 방식과 비교해 갖는 장점을 서술하시오.

### 110. ★★★ ResNet에서 잔차 블록의 출력이 (F(x)+x)일 때 Skip connection이 역전파 과정에서 기울기 전달에 유리한 이유를 설명하시오. 또한 초기화가 잘못되면 깊은 층의 출력 분산이 증폭될 수 있는 이유도 함께 설명하시오.

### 111. ★★ 강의자료에서 ResNet이 ReLU 계열 구조와 함께 He 초기화를 사용하는 이유를 설명하고, 초기 잔차 블록이 입력과 유사하게 동작하도록 만드는 초기화 전략을 서술하시오.

### 112. ★★★ 일반 합성곱과 MobileNet의 Depthwise Separable Convolution을 연산 구조 관점에서 비교하시오. Depthwise 단계와 Pointwise 단계가 각각 어떤 정보를 처리하는지도 설명하시오.

### 113. ★★ RNN에서 현재 은닉 상태 h_t가 현재 입력 x_t와 이전 은닉 상태 h_(t-1)에 의해 결정되는 이유를 설명하고, 마지막 h_t를 분류 출력에 사용하는 경우 어떤 정보가 담기기를 기대하는지 서술하시오.

### 114. ★★★ 기본 RNN에서 tanh가 비선형 함수임에도 긴 시퀀스에서 기울기 소실이 발생할 수 있는 이유를 시간축 역전파 관점에서 설명하시오.

### 115. ★★★ LSTM에서 Forget gate의 값이 1에 가깝거나 0에 가까울 때 이전 cell state 정보가 각각 어떻게 처리되는지 설명하고, Input gate 및 Output gate와의 역할 차이도 서술하시오.

### 116. ★★ ViT에서 이미지를 패치로 분할한 뒤 위치 인코딩을 추가하지 않는다면 어떤 정보가 부족해질 수 있는지 설명하고, CNN의 지역적 필터 처리와 비교하시오.

### 117. ★★★ CNN의 수용영역과 ViT의 Self-Attention을 비교하여 멀리 떨어진 이미지 영역 사이의 관계를 모델링하는 방식의 차이를 설명하시오.

### 118. ★★ ReLU, Leaky ReLU, ELU의 음수 입력 처리 방식을 비교하고, ReLU의 개선형 활성화 함수를 고려할 수 있는 상황을 설명하시오.

### 119. ★★ L1 정규화와 L2 정규화가 가중치에 주는 패널티의 형태와 학습 결과의 차이를 비교하고, 희소한 모델을 원할 때 어느 방식을 선택하는지 설명하시오.

### 120. ★★★ 초기 학습률이 0.1이고 30 epoch마다 0.1배가 되는 계단식 학습률 스케줄을 사용한다고 하자. 0~89 epoch의 학습률 변화를 설명하고, 이를 코사인 감소 및 선형 감소와 비교한 뒤 검증 성능이 악화될 경우 Early Stopping을 함께 사용하는 이유를 서술하시오.


## 이 문제 이렇게 공부해요
객관식 계산 문제는 출력 크기 공식, 파라미터 수, float32 메모리, FLOPs를 직접 손으로 반복 계산하세요. RNN/LSTM/ViT는 데이터가 각 블록을 어떤 순서로 흐르는지 그림 없이 설명할 수 있어야 합니다. 모델 변천사는 AlexNet → VGGNet → ResNet → MobileNet 순서로 핵심 아이디어와 해결하려던 문제를 연결해서 암기하세요.
