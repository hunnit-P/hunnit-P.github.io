# PART 1. 객관식 59문제

## A. 파운데이션 모델의 개념과 학습 패러다임

### 1.

강의자료에서 파운데이션 모델(Foundation Model)을 가장 적절하게 설명한 것은?

① 특정 한 가지 태스크만 수행하도록 소규모 데이터로 학습한 모델
② 대규모 데이터로 사전학습한 뒤 다양한 문제와 작업에 범용적으로 적용할 수 있는 기반 AI 모델
③ 모든 데이터를 데이터베이스에 저장한 뒤 최근접 이웃 검색만 수행하는 모델
④ 학습 때 본 데이터에 대해서만 동작하도록 설계된 모델

### 2.

강의자료에서 현실적인 기계학습 모델의 학습을 설명할 때 강조한 관점은?

① 모든 학습 데이터를 그대로 저장하는 과정
② 데이터를 패턴화하여 압축하고 표현을 학습하는 과정
③ 데이터의 레이블을 제거하는 과정
④ 모델의 파라미터를 모두 0으로 만드는 과정

### 3.

Representation Learning과 가장 관련이 깊은 설명은?

① 데이터의 유사점과 차이 및 패턴을 익혀 새로운 데이터에 일반화하는 능력을 얻는 것
② 모든 데이터를 원본 그대로 저장하는 것
③ 새로운 입력이 들어올 때마다 모델을 처음부터 다시 학습하는 것
④ 출력 클래스를 하나로 제한하는 것

### 4.

강의자료에서 파운데이션 모델의 특징으로 제시되지 않은 것은?

① 대규모 사전학습
② 높은 적응성
③ 다양한 작업에 대한 범용성
④ 항상 단일한 고정 출력 클래스만 지원

### 5.

기존 딥러닝 개발 패러다임과 비교한 파운데이션 모델 패러다임의 특징으로 가장 적절한 것은?

① 매 태스크마다 반드시 모델을 처음부터 학습한다.
② 거대 모델과 대규모 데이터 학습을 바탕으로 새로운 작업에 빠르게 적응한다.
③ 데이터가 많을수록 새로운 작업 적응이 불가능해진다.
④ 사전학습을 사용하지 않는다.

### 6.

파운데이션 모델 기반 개발 프로세스의 일반적인 흐름으로 가장 적절한 것은?

① Deployment → Training → Data Creation → Adaptation
② Data Creation → Data Curation → Training → Adaptation → Deployment
③ Training → Deployment → Data Curation → Data Creation
④ Adaptation → Data Creation → Deployment → Training

### 7.

강의자료에서 Zero-shot을 가장 적절하게 설명한 것은?

① 예제 몇 개를 추가로 학습한 뒤 적용하는 방식
② 특정 태스크의 전체 학습 데이터를 이용해 모델 가중치를 변경하는 방식
③ 처음 보는 문제를 추가 학습 없이 모델의 기존 지식을 활용해 바로 수행하는 방식
④ 모델을 무작위 초기화한 뒤 처음부터 학습하는 방식

### 8.

Few-shot의 설명으로 가장 적절한 것은?

① 예제 몇 개를 제공하여 새로운 작업에 적용하는 방식
② 어떠한 예제도 제공하지 않는 방식
③ 모델의 모든 파라미터를 삭제하는 방식
④ 데이터베이스 검색만 수행하는 방식

### 9.

Fine-tuning에 대한 설명으로 가장 적절한 것은?

① 모델 가중치는 절대 변하지 않는다.
② 특정 태스크에 맞게 추가 학습하여 모델 자체의 가중치를 업데이트할 수 있다.
③ 반드시 예제 없이 수행한다.
④ 사전학습보다 항상 더 큰 데이터셋이 필요하다.

### 10. ★

강의자료의 관점에서 파운데이션 모델 시대 AI 개발의 변화로 가장 적절한 것은?

① 모델을 태스크별로 새로 학습하는 능력만 중요해졌다.
② 잘 학습된 모델을 목적에 맞게 얼마나 잘 활용하고 적응시키는지가 중요해졌다.
③ 데이터와 계산 자원은 더 이상 필요하지 않다.
④ 사전학습 모델은 다운스트림 태스크에 사용할 수 없다.

## B. 시각언어모델(VLM)과 멀티모달 모델

### 11.

강의자료에서 인간의 지능을 설명할 때 사용한 관계는?

① cognition = perception만
② cognition = language만
③ cognition = perception과 higher cognitive processes의 결합
④ cognition = database search

### 12.

대규모 언어모델만으로 현실 세계를 이해하는 데 한계가 있다고 설명한 핵심 이유는?

① 언어모델은 텍스트를 전혀 처리하지 못하기 때문에
② 사고능력과 언어능력만으로는 시각을 포함한 현실 세계의 모든 정보를 충분히 이해하기 어렵기 때문에
③ 이미지에는 정보가 없기 때문에
④ 언어모델은 파라미터를 갖지 않기 때문에

### 13.

시각언어모델(VLM)의 기본 아이디어로 가장 적절한 것은?

① 이미지 인코더에서 얻은 시각 정보를 언어모델이 처리할 수 있는 형태로 연결한다.
② 이미지의 모든 픽셀을 문자열로 변환해 데이터베이스에 저장한다.
③ 언어모델을 제거하고 이미지 인코더만 사용한다.
④ 텍스트 입력을 사용하지 않는다.

### 14.

GPT-4 계열의 시각언어 모델 예시에서 강조한 확장 능력은?

① 자연어만 처리
② 이미지, 문서, 음성 등 멀티모달 데이터 처리
③ 숫자 데이터만 처리
④ 오프라인 데이터베이스 검색만 수행

### 15.

강의자료의 Sketch-to-web page 사례가 보여주는 능력은?

① 손으로 그린 웹페이지 스케치를 이해하여 HTML/JS 형태의 구현으로 연결
② 웹페이지를 이미지 파일로만 압축
③ 텍스트를 음성으로만 변환
④ 이미지의 색상 수만 계산

### 16.

VLM에서 이미지 인코더의 역할로 가장 적절한 것은?

① 이미지를 시각 특징 또는 임베딩으로 변환
② 최종 자연어 문장을 직접 출력하는 유일한 모듈
③ 모델의 모든 텍스트 토큰을 삭제
④ 이미지를 데이터베이스에 저장

### 17. ★

VLM 구조에서 시각 특징과 언어모델 사이의 projection 또는 connector가 필요한 주된 이유는?

① 이미지 파일 크기를 늘리기 위해
② 시각 표현을 언어모델이 사용할 수 있는 표현 공간 또는 토큰 형태로 연결하기 위해
③ 언어모델의 파라미터를 0으로 만들기 위해
④ 이미지 인코더를 제거하기 위해

### 18.

멀티모달 모델의 의미로 가장 적절한 것은?

① 한 종류의 입력만 처리하는 모델
② 이미지, 텍스트, 음성 등 서로 다른 modality의 정보를 함께 처리할 수 있는 모델
③ 오직 이미지 분류만 수행하는 모델
④ 데이터가 없는 모델

## C. sVLM과 온디바이스 모델

### 19.

sVLM을 개발하는 주요 목적과 가장 가까운 것은?

① 더 많은 GPU 메모리를 사용하기 위해
② 모바일·엣지 등 제한된 자원에서도 멀티모달 모델을 실행하기 위해
③ 모델의 입력을 텍스트로만 제한하기 위해
④ 클라우드 서버 사용을 의무화하기 위해

### 20.

강의자료에서 sVLM 경량화가 sLLM의 흐름과 연결된다고 설명한 이유는?

① 대규모 모델을 더 작고 효율적으로 만들어 제한된 환경에서 실행하려는 시도가 공통적이기 때문에
② 두 모델 모두 이미지를 사용하지 않기 때문에
③ sVLM은 파라미터가 항상 0개이기 때문에
④ sLLM은 반드시 VLM보다 크기 때문에

### 21.

SmolVLM을 개발한 곳은?

① Meta
② Hugging Face
③ NAVER
④ Kakao

### 22.

강의자료의 SmolVLM 구조에서 Vision Encoder로 표시된 것은?

① SigLIP
② SAM
③ Grounding DINO
④ LMDeploy

### 23.

SmolVLM 구조에서 시각 인코더 뒤에 위치하여 시각 특징을 언어모델 쪽으로 전달하는 구성은?

① Modality projection + Pooling
② Object database
③ OCR engine only
④ Diffusion decoder

### 24.

SmolVLM 관련 그래프가 강조하는 장점은?

① 비교 모델보다 추론 시 GPU RAM 요구량이 작음
② 모든 경우 가장 많은 GPU RAM을 사용
③ 이미지를 두 장 입력하면 메모리가 감소
④ GPU를 전혀 사용하지 않음

### 25.

Moondream 0.5B가 특히 염두에 둔 실행 환경은?

① 모바일 기기와 엣지 디바이스
② 대형 슈퍼컴퓨터만
③ 메인프레임 데이터베이스만
④ 브라우저 없는 서버만

### 26.

강의자료에 제시된 Moondream 모델의 파라미터 규모는?

① 약 2억 개
② 약 20억 개
③ 약 200억 개
④ 약 2조 개

### 27.

Moondream의 8비트 양자화 시 다운로드 크기로 제시된 값은?

① 47.9MB
② 479MB
③ 4.79GB
④ 47.9GB

### 28.

Moondream의 4비트 양자화 시 실행 메모리 수준으로 제시된 값은?

① 81.6MB
② 816MB
③ 8.16GB
④ 81.6GB

### 29.

양자화(Quantization)의 설명으로 가장 적절한 것은?

① 모델 파라미터의 정밀도 비트 수를 낮춰 크기와 메모리 사용을 줄이는 기술
② 학습 데이터를 모두 삭제하는 기술
③ 모델 파라미터 수를 항상 두 배로 늘리는 기술
④ 이미지를 텍스트로 바꾸는 기술

### 30.

강의자료에서 양자화의 장점으로 언급되지 않은 것은?

① 모델 파일 크기 감소
② 메모리 사용량 감소
③ 실행 속도 향상 가능
④ 정밀도 저하 가능성을 완전히 제거

### 31.

양자화의 단점으로 제시된 것은?

① 정밀도가 낮아지면서 일부 정확도와 품질이 소폭 감소할 수 있음
② 반드시 메모리 사용량이 증가함
③ 모델 파일 크기가 항상 증가함
④ 온디바이스 실행이 불가능해짐

### 32.

다음 중 Moondream이 제공하는 기능으로 강의자료에 포함되지 않은 것은?

① Image Captioning
② Visual Question Answering
③ Object Detection
④ 음성 합성 전용 기능

### 33.

Moondream의 제공 기능 중 이미지상의 좌표와 직접 관련된 것은?

① Pointing (x, y)
② Sentiment Analysis
③ Speech Recognition
④ Machine Translation

### 34.

Moondream의 제공 기능 중 문서 이미지 처리와 가장 관련 있는 것은?

① OCR & Document Understanding
② Gaze Detection
③ Pointing
④ Image Captioning만

### 35.

Gemini Nano의 핵심 특징으로 가장 적절한 것은?

① 온디바이스용 경량 Gemini
② 클라우드에서만 실행되는 초대형 모델
③ 이미지 처리가 불가능한 검색 엔진
④ 객체 탐지 전용 모델

### 36.

강의자료에서 Gemini Nano의 두 변형에 대해 제시한 파라미터 규모는?

① 1.8억과 3.25억
② 18억과 32.5억
③ 180억과 325억
④ 1.8조와 3.25조

### 37.

Gemini Nano가 탑재된 사례로 강의자료에서 언급된 스마트폰 시리즈는?

① Pixel 9
② Galaxy S10
③ iPhone 8
④ Nexus 5

### 38.

갤럭시 온디바이스 AI의 설명으로 가장 적절한 것은?

① 모바일 NPU를 활용해 이미지, 언어, 오디오, 영상 관련 생성형 AI 작업을 기기 내에서 직접 수행
② 모든 작업을 반드시 원격 GPU 서버에서 수행
③ 이미지 생성만 가능하고 언어 처리는 불가능
④ 오직 OCR만 수행

### 39.

강의자료의 갤럭시 온디바이스 AI 사례가 아닌 것은?

① 텍스트 기반 이미지 생성
② 인페인팅 및 아웃페인팅
③ 어조 변환 및 문법 교정
④ 데이터베이스 인덱스 재구축

### 40.

Qwen 2.5-VL 3B의 특징으로 제시된 것은?

① 다양한 크기의 이미지와 장시간 영상 처리
② 텍스트 입력만 처리
③ OCR을 지원하지 않음
④ 3D 렌더링 전용

### 41.

Phi-3.5 vision instruct 4.2B가 특화된 영역으로 제시된 것은?

① OCR, 차트 분석, 비디오 요약
② 오디오 합성만
③ 강화학습 환경 제어만
④ 데이터베이스 압축만

### 42.

DeepSeek-VL2 1B와 관련된 설명으로 가장 적절한 것은?

① 중국 AI 스타트업이 개발한 오픈소스 LLM 및 VLM 계열
② Meta의 이미지 분할 전용 모델
③ NAVER의 한국어 전용 검색엔진
④ Apple의 음성 전용 모델

### 43.

Gemma 3에 대한 설명으로 맞는 것은?

① Google의 멀티모달 오픈소스 모델 계열이며 다양한 크기의 모델을 제공
② 오직 1B 모델만 존재
③ 이미지 입력을 전혀 처리하지 못함
④ Grounding DINO와 동일한 모델

## D. 한국어 sVLM과 토큰화

### 44.

언어별 토큰화 길이가 달라지는 핵심 이유로 강의자료가 강조한 것은?

① 언어의 구조적·형태적 차이와 토크나이저의 언어 편향
② 모든 언어의 문장 길이가 완전히 같기 때문
③ GPU 종류만 다르기 때문
④ 이미지 해상도 차이 때문

### 45.

영어 중심 토크나이저에서 비영어권 언어가 겪을 수 있는 문제는?

① 같은 내용을 표현해도 더 긴 토큰 시퀀스가 생성되어 컨텍스트 활용 효율이 낮아질 수 있음
② 항상 영어보다 토큰 수가 적어짐
③ 텍스트가 자동으로 이미지가 됨
④ 모델 파라미터가 자동으로 증가함

### 46.

강의자료에서 영어가 일부 언어보다 높은 토큰 정보 밀도를 보일 수 있다고 설명한 의미는?

① 같은 토큰 길이로 더 많은 내용을 담을 수 있다는 뜻
② 영어는 토큰을 사용하지 않는다는 뜻
③ 영어는 모든 문장을 한 토큰으로 표현한다는 뜻
④ 영어 모델에는 컨텍스트 길이 제한이 없다는 뜻

### 47.

형태소가 복잡한 언어에서 토큰 수가 크게 증가할 수 있는 이유는?

① 하나의 단어가 길거나 여러 의미 요소를 결합하여 서브워드 단위로 많이 쪼개질 수 있기 때문
② 단어가 항상 한 글자이기 때문
③ 토크나이저가 숫자만 처리하기 때문
④ 문장부호가 없기 때문

### 48.

한국어 특화 토크나이저를 확장하여 기대할 수 있는 효과로 가장 적절한 것은?

① 한국어 문장을 더 적은 토큰으로 표현하여 처리 효율을 높일 수 있음
② 모든 한국어 문장을 반드시 한 토큰으로 만들 수 있음
③ 모델의 시각 인코더가 불필요해짐
④ 언어모델의 파라미터가 자동으로 0이 됨

### 49.

HyperCLOVAX-SEED-Vision-Instruct-3B를 개발한 곳은?

① NAVER
② Kakao
③ Meta
④ IDEA Research

### 50.

HyperCLOVAX-SEED-Vision-Instruct-3B의 설명으로 가장 적절한 것은?

① 한국어에 특화된 멀티모달 모델로 텍스트와 이미지를 함께 이해하고 텍스트를 생성
② 이미지 분할만 수행하는 모델
③ 영어 음성 인식 전용 모델
④ 객체 탐지 박스만 출력하는 모델

### 51.

Kanana-1.5-v-3b-instruct를 개발한 곳은?

① Kakao
② NAVER
③ OpenAI
④ Microsoft

### 52.

한국어 특화 VLM의 필요성과 가장 직접적으로 연결되는 문제는?

① 영어 중심 토크나이저에서 한국어의 토큰 효율이 낮아질 수 있음
② 한국어에는 이미지가 존재하지 않음
③ 한국어는 멀티모달 데이터를 사용할 수 없음
④ 한국어 문장은 항상 영어보다 짧음

## E. LMDeploy와 VLM 배포

### 53.

LMDeploy를 가장 적절하게 설명한 것은?

① LLM/VLM의 효율적 압축, 배포, 서빙을 지원하는 오픈소스 툴킷
② 이미지 라벨링 전용 웹사이트
③ 데이터베이스 관리 시스템
④ 운영체제 커널

### 54.

강의자료에서 LMDeploy의 효율적인 추론 기능과 관련해 언급한 것은?

① 지속적 배치, 병렬화, 고성능 CUDA 커널
② 모든 연산을 CPU 한 코어로 제한
③ 네트워크를 항상 비활성화
④ 모델을 매 요청마다 재학습

### 55.

LMDeploy의 효과적인 양자화에 대한 설명으로 맞는 것은?

① 4비트 양자화 등의 방식으로 더 빠른 추론을 지원할 수 있음
② 양자화는 지원하지 않음
③ 64비트만 지원
④ 양자화 시 모델 크기가 반드시 증가

### 56.

LMDeploy의 분산 서빙이 의미하는 것은?

① 여러 머신과 GPU에서 다중 모델 서비스를 배포할 수 있음
② 한 대의 CPU에서만 실행
③ 오직 로컬 파일 저장만 수행
④ 이미지 한 장만 처리

### 57.

LMDeploy의 대화형 추론 최적화와 관련된 설명은?

① 대화 이력 재처리를 줄이는 캐싱 등을 통해 효율을 높일 수 있음
② 대화 이력을 매번 삭제해야 함
③ 캐싱을 사용하면 반드시 추론이 느려짐
④ 대화형 모델은 지원하지 않음

## F. 이미지 파운데이션 모델

### 58.

이미지 파운데이션 모델의 설명으로 가장 적절한 것은?

① 컴퓨터 비전에서 방대한 데이터로 학습되어 분할, 탐지, 3D·깊이 예측 등 다양한 작업에 활용 가능한 모델
② 이미지 한 장을 외워서 동일 이미지에만 동작하는 모델
③ 텍스트 생성만 가능한 모델
④ 데이터베이스 검색만 가능한 모델

### 59. ★★

Set of Mark(SoM) 프롬프팅의 핵심 아이디어로 가장 적절한 것은?

① 이미지의 객체나 영역에 식별 가능한 마크/번호를 부여해 VLM의 시각적 grounding과 지시 이해를 돕는다.
② 모든 객체를 이미지에서 제거한다.
③ 이미지 해상도를 무조건 절반으로 줄인다.
④ 언어모델을 사용하지 않는다.

# PART 2. 단답형 15문제

## A. 핵심 개념

### 60.

대규모 데이터로 사전학습한 뒤 다양한 다운스트림 작업에 범용적으로 활용되는 기반 모델을 무엇이라고 하는가?

### 61.

추가 학습 없이 모델의 기존 지식만으로 새로운 작업을 수행하는 방식을 무엇이라고 하는가?

### 62.

몇 개의 예시를 제공하여 새로운 작업을 수행하게 하는 방식을 무엇이라고 하는가?

### 63.

특정 태스크 데이터로 모델의 가중치를 추가 학습하여 조정하는 방식을 무엇이라고 하는가?

### 64.

이미지와 텍스트 등 서로 다른 modality를 함께 이해하고 처리하는 모델을 일반적으로 무엇이라고 하는가?

## B. sVLM·온디바이스·한국어 모델

### 65.

SmolVLM의 Vision Encoder로 강의자료에 제시된 모델 이름은 무엇인가?

### 66.

모델 파라미터의 정밀도 비트 수를 낮춰 모델 크기와 메모리 사용량을 줄이는 기술은 무엇인가?

### 67.

Moondream 0.5B의 파라미터 수는 약 몇 개인가?

### 68.

Google의 온디바이스용 경량 Gemini 모델 이름은 무엇인가?

### 69.

NAVER가 개발한 한국어 특화 멀티모달 3B 모델의 이름을 쓰시오.

### 70.

Kakao가 개발한 한국어 특화 멀티모달 모델의 이름을 쓰시오.

### 71.

LLM/VLM의 효율적 압축, 배포, 서빙을 지원하는 오픈소스 툴킷 이름은 무엇인가?

## C. 이미지 파운데이션 모델

### 72.

Meta가 개발했으며 클릭, 박스, 부분 세그먼트, 텍스트 등의 입력을 받아 원하는 영역의 마스크를 추출하는 이미지 분할 파운데이션 모델은 무엇인가?

### 73.

텍스트 입력을 통해 이미지 내 물체를 탐지하며 높은 일반화 성능을 목표로 하는 IDEA Research의 모델은 무엇인가?

### 74.

Grounding DINO와 SAM을 결합하여 텍스트 입력으로 객체 탐지와 분할을 함께 수행하는 모델은 무엇인가?

# PART 3. 서술형 10문제

### 75. ★

파운데이션 모델과 기존 태스크별 딥러닝 모델의 개발 패러다임 차이를 대규모 사전학습, 적응성, 범용성의 관점에서 설명하시오.

### 76. ★

Zero-shot, Few-shot, Fine-tuning의 차이를 추가 데이터의 사용 방식과 모델 가중치 변경 여부를 중심으로 비교하여 설명하시오.

### 77. ★★

VLM의 일반적인 구조에서 이미지 인코더, projection/connector, 언어모델이 각각 어떤 역할을 수행하는지 설명하시오.

### 78. ★★

sVLM이 모바일·엣지 환경에서 중요한 이유를 모델 크기, 메모리, 추론 속도, 양자화의 장단점과 연결하여 설명하시오.

### 79. ★

영어 중심 토크나이저가 한국어 등 비영어권 언어에서 비효율을 만들 수 있는 이유와 언어 특화 토크나이저가 이를 어떻게 개선할 수 있는지 설명하시오.

### 80. ★

LMDeploy가 VLM 배포에 유용한 이유를 효율적 추론, 양자화, 분산 서빙, 대화형 추론의 관점에서 설명하시오.

### 81. ★★

SAM과 Grounding DINO의 입력과 출력 및 역할 차이를 설명하고, 두 모델을 결합한 Grounded SAM이 어떤 장점을 갖는지 설명하시오.

### 82. ★

Set of Mark(SoM) 프롬프팅이 일반 이미지 입력만 사용하는 것보다 VLM의 visual grounding을 개선할 수 있는 이유를 설명하시오.

### 83. ★★

한국어 특화 sVLM이 필요한 이유를 토큰 정보 밀도, 토큰 길이, 컨텍스트 효율, 한국어 멀티모달 이해의 관점에서 설명하시오.

### 84. ★★

이미지 파운데이션 모델이 기존 특정 태스크 전용 비전 모델과 비교해 갖는 장점을 일반화 성능과 다양한 다운스트림 작업 활용 관점에서 설명하고, SAM·Grounding DINO·Grounded SAM을 예로 들어 설명하시오.
